"""工具函数---# 通用辅助函数，如角度转换，提供给其他模块使用，避免重复代码"""
"""
角度转换：
    函数将度数和弧度相互转换。虽然 Python 的 math.radians 和 math.degrees 可用，但utils模块可以进一步封装，
    如接受角度字符串（"30deg", "0.52rad"）解析为浮点值，方便配置中既能用度也能用弧度表示角度。
向量计算：
    在光线路径计算中，可能需要基础的向量、坐标计算函数，utils模块可以提供如计算二维向量的单位化、旋转某角度的方向向量等函数，
    供 core 模块使用。
日志封装：
    如果项目需要日志记录（比如打印计算得到的角度、生成的图像路径等信息），utils可以封装 Python 内置 logging 模块的配置。
    例如提供 get_logger() 函数返回配置好的日志记录器，统一格式和等级。初学者可以在此学习如何管理日志输出。
其它：
    任何在多个模块间通用的功能都可以放在 utils，例如文件路径处理、缓存机制等（视项目复杂度而定）。
    由于本项目规模不大，utils应保持精简，将核心逻辑留给 core 和 visualization 等主要模块。
"""




